server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  #TODO learn more about spring.datasource and the below properties
  datasource:
    password:
    url:
    username:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
